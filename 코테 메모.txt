flag: 고기 먹기까지 얼마나 시간이 걸렸는지.

물고기를 먹은 경우의 layer를 다 돈다음 다음에 count+=1 이 되면서
if count>flag가 발동하여 while q:문이 break된다. > 그다음 if문 발동됨.

<치즈>
시작점 paper[0][0]로 계속할 수 없다. 불필요한 반복 많아질 뿐더러 딱 필요한 건 0으로 된 지점에서 이미 더해진 값(접한수만큼 1씩 더해진 값) 에 추가로 0인 부분만 고려해서 더하면 된다.
q_cheese_next에 이번에 사라진 치즈 위치(0으로 표시된 애들) 을 넣어줘서 다음 턴에 시작하는 지점으로 사용한다.

모범답안과 다른 점은 q=q_next해준 시점이 위에 있고, 이로 인해 q_next의 초기화가 반복된다는 점이다.
이를 해결하기 위해 모범답안 처럼 q=q_next를 마지막 부분에 해주면 q_next의 초기화 반복을 피할 수 있다. 

2667 단지 번호 문제
house_wide 이거 argument화 시킬 수 없나?
근데 힘들 것 같다. argument를 global 변수로 지정할 수 없다.
main 출력부에서 solution(n, map)을 새로운 array로 받고 이걸 활용하자.
함수 2번 여러번 출력하면 그만큼 실행시간 늘어난다. 한번만 실행시키자.